### 题目一:[20. 有效的括号](https://leetcode-cn.com/problems/valid-parentheses/) (简单)




### 题目二: [232. 用栈实现队列](https://leetcode-cn.com/problems/implement-queue-using-stacks/) (简单)


### 题目三: [225. 用队列实现栈](https://leetcode-cn.com/problems/implement-stack-using-queues/) (简单)


### 题目四:[71. 简化路径](https://leetcode-cn.com/problems/simplify-path/)


**思路: 为**`**..**`**就推出栈, 不为空或不为**`**.**`**则推入栈.**

**代码**

```javascript
/**
 * @param {string} path
 * @return {string}
 */
var simplifyPath = function(path) {
    const names = path.split('/');
    const stack = [];
    for (const name of names) {
        if (name === '..') {
            if (stack.length) {
                stack.pop();
            }
        } else if (name.length && name !== '.') {
            stack.push(name);
        }
    }
    return '/' + stack.join('/');
};
```



### 题目五: [150. 逆波兰表达式求值](https://leetcode-cn.com/problems/evaluate-reverse-polish-notation/) (计算逆波兰表达式)
**思路:**

逆波兰表达式严格遵循「从左到右」的运算。计算逆波兰表达式的值时，使用一个栈存储操作数，从左到右遍历逆波兰表达式，进行如下操作：

+ 如果遇到操作数，则将操作数入栈；
+ 如果遇到运算符，则将两个操作数出栈，其中先出栈的是右操作数，后出栈的是左操作数，使用运算符对两个操作数进行运算，将运算得到的新操作数入栈。

整个逆波兰表达式遍历完毕之后，栈内只有一个元素，该元素即为逆波兰表达式的值。



**代码**

```javascript
/**
 * @param {string[]} tokens
 * @return {number}
 */
var evalRPN = function(tokens) {
    const list = ['+', '-', '*', '/'];
    const stack = [];

    for (const s of tokens) {
        if (list.includes(s)) {
            const a = stack.pop();
            const b = stack.pop();
            let res;
            switch (s) {
                case '+':
                    res = b * 1 + a * 1;
                    break;
                case '-':
                    res = b - a;
                    break;
                case '*':
                    res = b * a;
                    break;
                case '/':
                    res = b / a > 0 ? Math.floor(b / a) : Math.ceil(b / a);
                    break;
            }
            stack.push(res);
        } else {
            stack.push(s);
        }
    }
    return stack.pop();
};
```

### 
### 题目六:[227. 基本计算器 II](https://leetcode-cn.com/problems/basic-calculator-ii/) 
#### 解法一: 单栈
**思路:遇到+, -时, 将运算数字推入栈, 遇到+, - 时, 需要将计算值推入栈中, 最后计算栈内元素之和.**

**代码**

```javascript
/**
 * @param {string} s
 * @return {number}
 */
var calculate = function(s) {
    s = s.trim();
    const stack = [];
    let num = 0;
    const n = s.length;
    let preSign = '+';
    for (let i = 0;i < n;i++) {
        const v = s[i];
        if (!isNaN(Number(v)) && v !== ' ') {
            num = num * 10 + Number(v);
        }
        if (isNaN(Number(v)) || i === n - 1) {
            switch (preSign) {
                case '+':
                    stack.push(num);
                    break;
                case '-':
                    stack.push(-num);
                    break;
                case '*':
                    stack.push(stack.pop() * num);
                    break;
                case '/':
                    stack.push(stack.pop() / num | 0);
                    break;
            }
            preSign = v;
            num = 0;
        }
    }
    return stack.reduce((pre, cur) => pre + cur, 0)
}
```

#### 
#### 解法二: 双栈
**思路: 将计算数字和符号分别存入两个栈中nums,ops,到是最后一位厚着, 当前的运算符的优先级低于栈顶运算符的优先级时, 则现将栈内的运算符进行运算, 得出的结果再推入数值栈中.**

**代码**

```javascript
/**
 * @param {string} s
 * @return {number}
 */
var calculate = function(s) {
    s = s.trim();
    const map = {
        '+': 1,
        '-': 1,
        '*': 2,
        '/': 2
    }
    let nums = [];
    let ops = [];
    let num = 0;
    let n = s.length;

    for (let i = 0;i < n;i++) {
        let v = s[i];
        if (!isNaN(Number(v)) && v !== ' ') {
            num = num * 10 + Number(v);
            if (i === n - 1) {
                nums.push(num);
                num = 0;
                while (ops.length) {
                    cal(ops, nums);
                }
            }
        }
        if (isNaN(Number(v))) {
            nums.push(num);
            num = 0;
            if (ops.length && map[v] <= map[ops[ops.length - 1]]) {
                while (ops.length && map[v] <= map[ops[ops.length - 1]]) {
                    cal(ops, nums);
                }
            }
            ops.push(v);
        }
    }
    return nums[0];
};

function cal (ops, nums) {
    let opera = ops.pop();
    let after = nums.pop();
    let pre = nums.pop();
    let num;
    switch (opera) {
        case '+':
            num = pre + after;
            break;
        case '-':
            num = pre - after;
            break;
        case '*':
            num = pre * after;
            break;
        case '/':
            num = pre / after > 0 ? Math.floor(pre / after) : Math.ceil(pre / after);
            break;
    }
    nums.push(num);
}
```

