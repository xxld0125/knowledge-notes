### 一、快速实现[1,2,...100],你能想到的所有解：
**1、**

```javascript
function fn1 (n) {
    return Array(n).fill(0).map((item,index) => index + 1)
    // return Array(100).map((item,index) => index + 1) // Array(100)会生成100*empty,map会跳过empty;
}
```

**2、**

```javascript
function fn2 (n) {
    const res = [];
    for (let i = 0;i < n;i++) {
        res.push(i + 1);
    }
    return res;
}
```

**3、**

```javascript
function fn3 (n) {
    const res = Array(n).fill(0);
    res.forEach((item, index, arr) => {
        arr[index] = index + 1;
    })
    return res;
}
```

**4、**

```javascript
function fn4 (n) {
    const res = Array.from(Array(n)).map((item, index) => index + 1);
    return res;
}
```

**5、**

```javascript
function fn5 (n) {
    const res = [...Array(n)].map((item, index) => index + 1);
    return res;
}
```



**备注**：该题主要的知识点是`JavaScript`对数组空位的处理

1. 不识别空位的数组方法、
+ `forEach`, `filter`, `reduce`, `every` 和`some`都会跳过空位；
+ `map`会跳过空位，但会保留这个值；
+ `join`和`toString`会将空位视为`undefined`，而`undefined`和`null`会被处理成空字符串。  

2. 识别空位的数组方法
+ `Array.from`方法会将数组的空位，转为`undefined`，也就是说，这个方法不会忽略空位；
+ 扩展运算符（...）也会将空位转为`undefined`；
+ `copyWithin`会连空位一起拷贝；
+ `fill`会将空位视为正常的数组位置；
+ `for...of`循环也会遍历空位；
+ `entries`、`keys`、`values`、`find`和`findIndex`会将空位处理成`undefined`；





### 二、数组去重
**题目**

```javascript
const arr = [1,2,3,4,3,2,1];
```

**1、**

```javascript
function fn1 (arr) {
    return [... new Set(arr)]
}
```

**2、**

```javascript
function fn2 (arr) {
    const res = [];
    for (let i = 0;i < arr.length;i++) {
        if (!res.includes(arr[i])) {
            res.push(arr[i])
        }
    }
    return res
}
```

**3、**

```javascript
function fn3 (arr) {
    return arr.filter((item, index, arr) => index === arr.lastIndexOf(item))
}
```



### 三、 场景实现
```plain
用原生js实现，要求：不能搜索网上资源，做到组件化，时间100 min。
1. 实现一个div滑动的动画，由快至慢5s结束（不准用css3)。
2. 页面内有一个input输入框，实现在数组arr查询命中词并要求autocomplete效果。
题目非常简单，但是非常考验面试者的代码实现能力、设计能力，功能实现基本谁都能做到。 
```



### 四、大数相加
```plain
实现超出整数存储范围的两个大整数相加function add(a,b)。注意a和b以及函数的返回值都是字符串。
```



### 五、原生拖拽实现
```plain
- 通过 mousedown、mousemov`、mouseup 方法实现
- 通过 HTML5 的 Drag 和 Drop 实现
```

### 
### 六、隐式转换
```javascript
// 以上两种打印 success成功；
if (a == 1 && a == 2 && a == 3) {
    console.log(success)
}
if ( a === 1 && a === 2 && a === 3) {
    console.log(success)
}
```

1、

```javascript
let a = {
    _default:0,
    [Symbol.toPrimitive] () {
        return ++this._default;
    }
}
```

2、

```javascript
let a = {
    _default:0,
    valueOf() {
        return ++this._default;
    }
}
```

3、

```javascript
let a = {
    _default:0,
    toString() {
        return ++this._default;
    }
}
```

4、

```javascript
let _default = 0;
Object.defineProperty(window, 'a', {
    get() {
        return ++_default;
    }
})
```

### 
### 七、["1", "2", "3"].map(parseInt)
**结果**：我们期望输出 [1, 2, 3], 而实际结果是 [1, NaN, NaN].

**原因**：`parseInt`接收两个参数，所以`["1", "2", "3"].map(parseInt)`相当与`["1", "2", "3"].map(parseInt(item,index))`;

可实现的方法：

```javascript
["1", "2", "3"].map(item => parseInt(item, 10));
```



### 八、0.1 + 0.2 === 0.3吗，为什么？
进制转换，最大安全数、`IEEE754`;



### 九、后台发送一个超过安全数的数字，怎么办？
会发生精度丢失，超过16为的部位，会变为0处理；



### 十、this绑定的优先级
`new `> `bind`,`apply`,`call `> 对象调用 > 默认绑定(`window`);



### 十一、Symbol的作用
1. `Symbol`是独一无二的，可以命名变量，防止变量冲突；
2. `Symbol`属性，不会被一般的遍历方法获取到，可以模拟私有；
3. `[Symbol.Iterator]`可以给不可迭代数据类型提供迭代接口；



### 十二、变量提升；
当执行一个函数时，首先会创建执行上下文，进行变量提升，绑定`this`，确定作用域链等；

其中关于变量部分，如下：

1、创建`arguments`属性(箭头函数没有)；

2、创建函数的形参，值为`undefined`；

3、创建函数声明，值为原函数的引用；

4、创建var声明的变量，值为`undefined`；

注：变量提升中函数声明的优先级高于`var`声明的同名变量；



### 十三、闭包；


### 十四、隐式转换：


### 十五、script标签的defer和async属性


### 十六、window.Onload和DOMContendLoaded对比


### 十七、如何判断obj对象为一个空对象
```javascript
Object.keys(obj).length === 0;

JSON.stringify(obj) === '{}'
```



**对象遍历方法**

1. `Object.keys()`、`Object.values()`、`Object.entries()`获取对象自身可枚举属性;
2. `for-in`:获取对象可枚举属性(包括继承的属性);
3. `Object.getOwnPropertyNames()`获取对象自身属性(包括不可枚举属性);
4. `Object.getOwnPropertySymbols()`获取自身的symbol属性;
5. `Reflect.ownKeys()`获取自身属性(包括不可枚举属性)；	



**JSON.stringify()实现深拷贝的缺点：**

1. 如果`obj`里面有时间对象，则`JSON.stringify`后再`JSON.parse`的结果，时间将只是字符串的形式。而不是时间对象；
2. 如果`obj`里有`RegExp`、`Error`对象，则序列化的结果将只得到空对象；
3. 如果`obj`里有函数，`undefined`，则序列化的结果会把函数或 `undefined`丢失；
4. 如果`obj`里有`NaN`、`Infinity`和`-Infinity`，则序列化的结果会变成`null`；
5. `JSON.stringify()`只能序列化对象的可枚举的自有属性，例如 如果obj中的对象是有构造函数生成的， 则使用`JSON.parse(JSON.stringify(obj))`深拷贝后，会丢弃对象的`constructor`；
6. 如果对象中存在循环引用的情况也无法正确实现深拷贝；



### 十八、原型、原型链及继承


### 十九、伪数组转数组
1. `Array.from(target)`;
2. `Array.prototype.slice.call(target)`;
3. `[].slice.call(target)`;
4. `[...target]`;
5. 循环方法；



### 二十、箭头函数与普通函数的区别
1. 更简洁的语法
2. 没有this
3. 不能使用new 构造函数
4. 不绑定arguments，可用rest参数...解决
5. 使用call()和apply()调用，但改变this指向无效；
6. 捕获其所在上下文的 this 值，作为自己的 this 值
7. 箭头函数没有原型属性(即prototype属性)
8. 不能简单返回对象字面量
9. 箭头函数不能当做Generator函数,不能使用yield关键字
10. 箭头函数不能换行



### 二十一、事件流
事件捕获 => 处于目标事件阶段 => 事件冒泡；

过程：

    父元素捕获 => 子元素冒泡 => 子元素捕获 => 父元素冒泡；

**注：**

目标元素的冒泡和捕获执行先后，根据其事件的绑定顺序而定；





### 二十二、事件如何实现
在页面加载时，将事件与对应的目标元素进行绑定，但目标元素触发对应的操作时，执行绑定的事件；



### 二十三、事件对象；


```javascript
event.target;
event.currentTarget;
preventDefault;
prevent(vue修饰符)；
```



### 二十四、beforeUnload
在页面关闭前执行；



### 二十五、Null是对象吗?
`null`不是对象，设计初期时，`000`代表对象，但是`Null`是全`0`，所以`typeof null` 也是`object`,历史遗留问题；



### 二十六、Object.is 和 === 的区别


```javascript
// 1、+ 0 和 - 0:
  +0 === - 0 //  true;
  Object.is(+0， -0) // false;
// 2、NaN 和 NaN:
  NaN === NaN // false;
  Object.is(NaN, NaN) // true;
```



### 二十七、[] == ![]
`==` 进行比较时，两边会转为`Number`类型；

`[]`转化为数字为`0`；

`![] => []`先转为`true`,`![]`为`false`；

所以` [] == ![]`;



### 二十八、对象的隐式转换流程
1. 调用`[Symbol.toPrimite]`；
2. `valueOf`；
3. `toString`；



### 二十九、基本包装类型
`Number`、`String`、`Boolean`三种基本数据类型为基本包装类型；

直接用字面量创建时，调用方法，如：

`'222'.split()`；

    首先会`new String()`;

    再调用方法；

    将调用结果赋值给目标变量；

    最后再删除new的对象；



### 三十、类型转换
`==` 运算符两边类型不一致时，会转化为数字进行比较：



1、转换为`boolean`：

    `+0`，`-0`，`NaN`, `undefined`，` ''`,`null `=> `false`;

    其他为`true`

2、转换为`Number`：

    `Array` =>`[] => 0`,` [1] => 1(单元素)`,`[1,2] => NaN`;

    `Object` =>`NaN`;

    `Boolean` =>`false => 0`;`true => 1`;

    `String` =>` '1' => 1`;`'1a' => NaN`;

    `Null` => `0`;

    `Undefined` => `NaN`;

   ` Fucntion` =>`NaN`;

3、转化为字符串：

    `Array`=>`[1,2,3] => '1,2,3'`

    `Function`=>`function fn () {} => 'function fn () {}'`

    `Boolean`=>`true => 'true'`;

    `Number`=> `1 => '1'`;

    `null`=>`'null'`;

    `undefined`=>`'undefined'`;

    `Object`=>`{} => '[object Object]'`

    `Symbol`=> `Symbol('a') => 'Symbol("a")'`;



### 三十一、迭代器
对象的`[Symbol.Iterator]`的属性；

有该属性，调用返回一个`Iterator`对象；

该对象有一个`next`方法；

调用返回含有`next`和`done`属性的对象；



### 三十二、常用遍历方法对比
+ `forEach`：

    使用范围：数组；

    不可中断：不可使用`break`(报错)、`continue`(报错)：

    需要中断的遍历场景，选择使用`some`、`every`；

    无返回值；



+ `for-in`：

    使用范围：对象；

    可以中断：`break`、`continue`；



+ `for-of`：

    使用范围：带有`[Symbol.iterator]`属性的对象；

    如：数组、字符串、`Map`、`Set`、`NodeList`等等；

    可以中断：`break`、`continue`



+ `map`

    不可中断：不可使用`break`(报错)、`continue`(报错)：

    返回一个新数组；



### 三十三、数组遍历方法对比


```javascript
forEach；
map：
some：
every：
reduce：
reduceRight：
fill：
find：
findIndex：
```



### 三十四、路由模式:
1. `hash`模式
2. `histroy`模式





### 三十五、模块化


### 三十六、垃圾回收


### 三十七、JS引擎


### 三十八、AST


### 三十九、Web Work


### 四十、不会冒泡的事件
1. `onblur`
2. `onfocus`
3. `onmounseenter`
4. `onmounseleave`



### 四十一、点击input触发的事件
1. `onmouseenter`
2. `onmouseover`
3. `onblur`
4. `onclick`



### 四十二、Null和undefined的区别
1、定义：  
`null `表示 无 的对象，也就是此处不应该有值；  
`undefined `表示未定义。  
在转换数字的时候，`Number(null) 为0`，而`Number(undefined)`为`NaN`。



使用场景细分如下：

+ `null`：
1. 作为函数的参数，表示该函数的参数不是对象。
2. 作为对象原型链的终点。`Object.prototype.proto === null`



+ `undefined`：
1. 变量被声明但是没有赋值，等于 `undefined`。
2. 调用函数时，对应的参数没有提供，也是 `undefined`。
3. 对象没有赋值，这个属性的值为 `undefined`。
4. 函数没有返回值，默认返回 `undefined`。





### 四十三、Document、window、html、body的层级关系
`window `> `Document `> `html `> `body`





### 四十四、如何把http请求换成https


### 四十五、http请求拦截
`ngix`、网关拦截



### 四十六、如何不使用框架实现组件的按需加载


### 四十七、浏览器为什么要跨域、而小程序为什么没有


### 四十八、开发一个模块需要考虑哪些问题


### 四十九、实现一个一组异步请求按顺序执行有哪些方法


### 五十、如何在前端实现图片压缩


### 五十一、class和构造函数的区别


### 五十二、RequestAnimationFrame和setTimeout的区别


### 五十三、V8如何执行一段JS代码


### 五十四、js对象循环引用会导致什么问题?
1. 当垃圾回收机制使用引用计数法时，无法回收变量循环引用的内存，会造成内存泄漏;
2. 循环引用的对象，使用`JSON.stringify()`会报错;



**解决方法：**

    一个自然的想法能不能消除循环引用，一个 `JSON `扩展包 做到了这一点， 使用` JSON.decycle `可以去除循环引用；







### 五十五、Base64图片优缺点
1、定义：

    图片的 `base64 `编码就是可以将一副图片数据编码成一串字符串，使用该字符串代替图像地址，可减少HTTP请求。

    但是使用 `base64 `的一个前提，那就是被 `base64 `编码的图片足够尺寸小;

2、使用场景：

    如果图片足够小且因为用处的特殊性无法被制作成雪碧图（`CssSprites`），在整个网站的复用性很高且基本不会被更新。

3、优缺点对比：

    1、优点：

        无需额外的HTTP请求；

        可像单独图片一样使用；

        没有跨域问题、无需考虑缓存等问题；

    2、缺点：

        `CSS`文件体积增大，导致`CRP`的阻塞；

        `CSS`文件体积增大，导致`CSSOM`生成的时间增加；



    注：`CRP`（`Critical Rendering Path`，关键渲染路径）：当浏览器从服务器接收到一个`HTML`页面的请求时，到屏幕上渲染出来要经过很多个步骤。浏览器完成这一系列的运行，或者说渲染出来我们常常称之为“关键渲染路径”。



### 五十六、Weak-Set、Weak-Map和Map、Set的区别


### 五十七、数据类型检测
+ `typeof `：数据检测缺点；无法检测`Array`、`Object`、`Null`几个数据类型，这几个类型均为`Object`；

    注：`typeof `会对 `null `显示错误是个历史 Bug，`typeof null` 输出的是 `object`，因为 `JavaScript `早起版本是 32 位系统，为了性能考虑使用低位存储变量的类型信息，`000 `开头代表是对象然而 `null `表示为全零，所以它错误判断为 `object`。



+ `Object.prototype.toString.call()`：万能的数据检测方法；



+ `instanceof`：

缺点：

    - 无法检测`undefiend`, `null`;
    - 当通过直接字面量创建基本类型数据时，无法通过`instanceof`检测数据类型；
    - 当检测数据的类存在原型的继承时，检测也未必准确；(原理同`constructor`)

         

+ `constructor`:

 缺点：

    - 无法检测`undefined`、`null`；
    - 当被检测的数据的原型指向另一个实例时，使用`constructor`检测的是这个实例的数据类型；不是被检测数据的数据类型；



+ 5、ES6新增的数据检测方法:
    - `Aray.isArray()`判断数据是否为数据类型



### 五十八、new的作用


### 五十九、组件寄生继承和class的extends继承的区别
1、`class`写法更简单，更接近传统语言的写法；

2、ES6继承：`C.__proto__ === F`(构造函数继承),但是ES5继承：`C.__proto__ !== F`;

3、ES6子类可以继承父类的静态方法；



### 六十、常用的原型检测方法
1. `对象.prototype`:设置构造函数的原型对象；
2. `_proto_`查看对象的原型对象；
3. `属性 in 指定对象` ： 查看指定对象是否在某对象的原型链上；
4. `对象.hasOwnProperty(属性)`,判断某对象是否有指定的私有属性 （返回布尔值）；
5. `对象 instanceof 指定对象` ： 判断指定对象的原型是否在对象的原型链上；
6. `指定对象 isPrototyOf 对象` ： 判断对象是否在指定对象的原型链上（指定该对象带`prototype`）;



### 六十一、函数式编程
**函数式编程的特点：**  
1. **函数是一等公民**。可以利用这点让它支持抽取到外部。  
2. **声明做某件时间**。函数式编程大多数声明某个函数需要做什么，而不是它怎么做的。  
3. **便于垃圾回收**。函数内部的变量方便垃圾回收，不会产生太多的变量，用户不需要大量的定义。  
4. **数据不可变**。函数式编程要求所有的数据都是不可变的，如果需要修改某个对象，应该新建后再修改，而不是污染原本的数据。  
5. **无状态**。不管什么时候运行，同一个函数对相同的输入返回相同的输出，而不依赖外部状态的变化。  
6. **无副作用**。功能 A 应该仅仅为了完成它的实现，而不会随着外部的改变而改变，这样当它执行完毕之后，就可以将其内部数据进行回收。并且它不会修改传入的参数。



**纯函数：**  
纯函数的概念有 2 点：  
1. **不依赖外部状态（无状态）**：函数的运行结果不依赖全局变量，`this` 指针，`IO` 操作等。  
2. **没有副作用（数据不变）**：不修改全局变量，不修改入参。  
优点：  
- 便于测试和优化  
- 可缓存性  
- 自文档化  
- 更少 Bug



### 六十二、onmouseover 和 onmouseenter 区别
这两个事件都是移入的时候触发，但是`onmouseover`会触发多次，而`onmouseenter`只有在进去的时候会触发;



### 六十三、柯里化
1、定义：

柯里化( `Currying`) 是把接受多个参数的函数转变为单一参数的函数，并返回接受余下的参数且返回结果的新函数的技术。

简单来说就是：通过闭包管理，支持链式调用，每次运行返回一个function。即：通过将多个参数转化为一个参数，每次运行返回新函数的技术；  
2、作用：

+  参数复用；
+ 提前确认；
+  延迟运行；



### 六十四、XML和JSON的区别
1. 数据体积方面。  
`JSON`相对于XML来讲，数据的体积小，传递的速度更快些。
2. 数据交互方面。  
`JSON`与`JavaScript`的交互更加方便，更容易解析处理，更好的数据交互。
3. 数据描述方面。  
`JSON`对数据的描述性比`XML`较差。
4. 传输速度方面。  
`JSON`的速度要远远快于`XML`。





