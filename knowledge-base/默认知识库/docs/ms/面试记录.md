### 目前计划：2020/12


#### 计划明年春节后开始找工作；


		现在开始学习面试学习准备



20210306：学习计划：



1、完成css部分复习：阅读完相关文档；(完成0306)



2、完成排序算法学习；(完成0306)



3、完成计算机网络及浏览器知识复习;(未开始，计划0307完成)



20210307：



1、完成计算机网络及浏览器知识复习;



2、完成查找算法，及排序算法复习;//完成;



3、完成简历，并发wm；//完成;



4、设计模式复习整理;



5、webpack复习整理;



### 面试记录：


#### 一、2021-03-19：伯医科技：


##### 1、一面(电话面试)：


1.  自我介绍； 
2.  项目介绍； 
3.  vue组件的name作用： 
    1. 实现递归组件；
    2. keep-alive中，includes、excludes通过匹配组件的name进行选择缓存；
    3. 浏览器使用vue-tools调试：调试工具里显示的组件名称是由vue中组件name决定的
4.  vue-router的路由守卫： 
    1. 全局守卫： 
        1. beforeEach：
        2. afterEach：
        3. beforeResolve:和 beforeEach 类似，区别是在导航被确认之前，**同时在所有组件内守卫和异步路由组件被解析之后**，解析守卫就被调用。
    2. 路由守卫： 
        1. beforeEnter：
    3. 组件内守卫： 
        1. beforeRouteEnter：
        2. beforeRouteLeave:
        3. beforeRouteUpdate:
    4. 完成的导航解析流程： 
        1. 导航被触发。
        2. 在失活的组件里调用 `beforeRouteLeave` 守卫。
        3. 调用全局的 `beforeEach` 守卫。
        4. 在重用的组件里调用 `beforeRouteUpdate` 守卫 (2.2+)。
        5. 在路由配置里调用 `beforeEnter`。
        6. 解析异步路由组件。
        7. 在被激活的组件里调用 `beforeRouteEnter`。
        8. 调用全局的 `beforeResolve` 守卫 (2.5+)。
        9. 导航被确认。
        10. 调用全局的 `afterEach` 钩子。
        11. 触发 DOM 更新。
        12. 调用 `beforeRouteEnter` 守卫中传给 `next` 的回调函数，创建好的组件实例会作为回调函数的参数传入。
5.  vue的inject/provide的原理： 
    1.  inject/provide的有无缺点对比 
        * 优点： 
            + 祖先组件不需要知道哪些后代组件使用它提供的属性；
            + 后代组件不需要知道被注入的属性来自哪里；
        * 缺点： 
            + 组件间的耦合较为紧密，不易重构；
            + 提供的属性是非响应式的；
    2.  源码分析： 
        1.  在组件实例初始化的时候会调用initInjections 和 initProvide； 
            1.  initInjections主要做了两件事： 
                1. 获取vm.$options.inject通过resolveInject方法找到对应的key集合；
                2. 遍历key集合，进行响应式监听；
            2.  resolveInject：  
根据注册的inject，通过$parent逐级向上查找对应的provide： 
            3.  initProvide：  
把注册的provide值，赋值给vm._provided, resolveInject中有使用到; 
1.  常用ES6特性： 
    - 变量命名：let、const、块级作用域
    - 解构赋值：数组元素调整位置，函数参数默认值设置，多重结构取值；
    - 字符串扩展：模板字符串、includes、startsWith、endsWith、repeat、padStart、padEnd、trimStart、trimEnd；
    - 数值扩展：isNaN、parseInt、parseFloat、isFinite、isIteger、bigInt、**；
    - 函数扩展：默认值、rest参数、严格模式、name属性、箭头函数、toString；
    - 数组扩展：...、Array.from、Array.of、copyWithin、find、findIndex、fill、entries、values、keys、includes、flat、flaMap；
    - 对象扩展：属性名简写、？？(Null判断运算符)、？.(链判断运算符)、... 、super(指向当前对象的原型对象)、属性表达式、Object.is、Object.assign、Object.getPrototypeOf(等同于**proto**)、Object.fromEntries、Object.keys、Object.values、Object.entries;
    - 新增数据类型：symbol、bigInt；
    - 新增数据结构：Set、Map、WeakSet、WeakMap；
    - proxy；
    - Reflect；
    - promise、async/await、generator；Promise的静态方法(all、race、any、then、catch、finally、allSettled、resolve、reject、try)
    - Iterator/for-of；
    - class
    - module
7.  完成一个全局loading的思路： 
    1. 新建一个loading的组件；
    2. 导入根组件，并增加开关(v-show)；
    3. 将开关控制添加到vuex；
    4. 通过改变vuex的值，实现全局loading的控制；
8.  class继承： 
9.  深拷贝，浅拷贝，深拷贝在项目中的应用,JSON实现深拷贝的缺点： 
    1.  JSON实现深拷贝的缺点： 
        1. 如果obj内有时间对象、JSON.stringify后再JSON.parse的结果，时间将只是字符串的形式；
        2. 如果obj里有RegExp(正则表达式的缩写)、Error对象，则序列化的结果将只得到空对象；
        3. 如果obj里有函数，undefined，则序列化的结果会把函数或 undefined丢失；
        4. 如果obj里有NaN、Infinity和-Infinity，则序列化的结果会变成null；
        5. JSON.stringify()只能序列化对象的可枚举的自有属性，例如 如果obj中的对象是有构造函数生成的， 则使用JSON.parse(JSON.stringify(obj))深拷贝后，会丢弃对象的constructor；
        6. 如果对象中存在循环引用的情况也无法正确实现深拷贝；
    2.  JSON的妙用： 
        1.  判断数组是否包含某对象，或者判断对象是否相等。 
        2.  判断两数组/对象是否相等 
        3.  让localStorage/sessionStorage可以存储对象； 
        4.  JSON.stringify()与toString()这两者虽然都可以将目标值转为字符串， 

```jsx
JSON.stringify([1,2,3])  =>  '[1,2,3]'
arr.toString() => '1,2,3'
```

 

10.  Less高级函数用法： 
    1.  变量： 
    2.  混入：可以将一个定好的样式，引入到另一个样式中，类似于函数的调用;  
先定义一个基本样式，设置样式参数，并设置默认值 
    3.  嵌套： 
    4.  运算：算术运算符 `+`、`-`、`*`、`/` 可以对任何数字、颜色或变量进行运算。  
clac(); 
    5.  转义：允许你使用任意字符串作为属性或变量值。 
    6.  函数： 
    7.  命名空间和访问服： 
    8.  映射： 
    9.  作用域： 
    10.  注释： 
    11.  导入： 
11.  keep-alive介绍： 
12.  vuex介绍： 
13.  组件传值方法： 
14.  有无微信小程序经验 
15.  有无Electron开发经验 



##### 2、二面(现场面试) 备注：面试官是学长、老乡


1.  自我介绍； 
2.  如何看待前端后面的发展； 
3.  最近看过哪些技术书籍； 
4.  事件触发原理、冒泡原理； 
5.  在页面上拖动一个组件涉及哪些知识； 
6.  有接触过node.js、Electron、Flutter吗； 
7.  现在项目遇到的页面渲染性能问题； 
8.  假设有个页面有十万各数据，要展示出来，怎么实现优化，一次性请求； 
9.  页面上只有9000数据，如何实现优化； 
10.  介绍下Vue的源码、JS实现的核心逻辑； 
11.  职业规划； 
12.  css怎么样，动画效果； 
13.  经常看哪些论坛； 
14.  有些过博客嘛； 
15.  HTTP请求状态码 
16.  薪资要求；  
面试结果:通过；薪资14k；双休、9-18；自研(规模：100-500)； 



#### 二、2021-03-20： 开思时代科技(外包)


1、自我介绍：



2、new的构造函数有return，返回什么，new的作用；



		1、没有return的情况下，默认返回新创建的对象；



		2、有return的情况下：



				1、返回值为基本数据类型，则返回新建对象实例，即this；



				2、如果是一个非基本数据类型，函数的返回值为指定的对象，this值所引用的对象会被丢弃;



3、浏览器缓存；



4、http返回的状态码；



5、while(true)，不使用return，如何避免是循环；



				break；



6、异步解决方法；



7、async返回什么；



8、async 函数 return 1 返回什么；



面试结果：通过;



情况：每个月大小周、技术栈React、工资每月约19k、无年终。。。；



#### 三、2021-03-22：VeSync(一面)


岗位要求：



1.精通HTML、CSS、Javascript,精通DIV+CSS网页布局，熟悉W3C标准，具有手写原生代码的能力；  
2.理解AJAX运行机制，熟悉掌握Vue、Angular、React、jQuery等一种或者多种前端框架，并具有实际项目开发经验；  
3.对常见的浏览器兼容问题有清晰的理解，并有可靠的解决思路和方案，如IE8/9/10、Edge、Chrome、Firefox等；  
4.对JavaScript基本对象、数据类型、作用域有充分了解，了解面向对象思想，有掌握ES6、ES7语法相关开发经验者优先；



面试内容：



1、自我介绍：



2、跨域方法：



	**1、JSONP实现，后端是如何调用传入的方法：**



	2、JSONP实现跨域的原理：



3、浏览器缓存：



**4、如何实现组件二次封装：**



5、工作中用到的性能优化：



6、回流和重绘原理；



7、CDN原理;



**8、webpack为什么要代码分割，对浏览器有什么好处：**



	webpack打包的本意是为了将多个http请求才能加载的文件用一个http请求实现，但如果只具备打包功能webpack就显得有些极端了， 因为如果把前端文件都完全打包的话， 首屏加载可能会很慢， 所以要用代码分割的功能减少一部分不必要打包的文件。 webpack的打包功能和代码分割功能可以看作是对立的，就像是天平的两端， 让你自己去实现两边的平衡。



9、对闭包、作用域及作用域链的理解；



10、webpack代码分割的方法；



11、实现对象赋值；



12、promise产生的任务是什么任务，promise.then的作用;



13、事件循环机制原理；



14、vue的生命周期，data初始化在哪个阶段？



**15、vue项目中的，assets文件夹和static文件夹之间的区别？**



	1、相同点：



			文件夹中的资源在html中使用都是可以的。



	2、不同点：



			在js中使用assets下面的资源的话，路径要经过webpack中的file-loader编译，路径不能直接写。



	3、static :  
在项目打包过程中，使用命令npm run build时，该目录下的文件不会被wabpack处理的，它们会被直接复制到最终的打包目录下面（默认是dist/static ），使用时必须使用绝对路径来引用这些文件，这是在 config.js 文件中的 build.assetsPublicPath 和 build.assetsSubDirectory 设置来确定的。



注意：任何放在 static 中的文件需要以绝对路径的形式引用：/static/[filename]



	4、assets：



在项目打包过程中，使用命令npm run build时，src目录下的assets 中的文件会经过 webpack 进行打包编译。



	5、总结：



		1、static中的文件，是不会经过编译的，打包后会生成dist文件夹，static中的文件是复制的static的文件。建议static目录中放一些外部的第三方的文件，自己的文件最好放在assets文件中。  
		2、若把图片放在assets和static中，html页面中都可以使用； 但是在动态绑定中，assets路径的图片会加载失败，因为webpack使用的是 commenJS 规范，必须使用require才可以。所以图片等信息最好放在static中。



17、BFC是什么、有什么作用：



#### 四、**明源云**


##### 1、2021-03-23：(笔试)


1、HTTP状态码写六个以上；



2、画出vue生命周期函数；



3、实现三行布局，优先级为第三行、第一行、第二行、三行高度：100px、auto、200px；



4、不使用while、for循环，使用Array自带的方法，实现有110个元素的数组，每个元素的等于数组长度加上各自下标；



5、本地有10w数据，有名称和类型两种值。如果实现前端筛选的优化，说明方案；



6、实现柯里化；



7、实现一个等边三角形；



##### 2、2021-03-30：(技术面：两面-基础面、项目面)


1. 
2. 



##### 3、2021-03-30：(部门负责人)


#### 五、2021-03-27：济济科技(技术面试-一面)


1、做笔试题



	图片正则、电话号码正则；



2、技术面试：



	1、介绍vue全家桶；



	2、ES5实现数组的map方法;



	3、原型链；



	4、实现权限管理；



	5、element-ui的树是否用过;



#### 六、2021-03-29：左邻


##### 1、笔试


##### 2、2021-03-29：技术面


**技术一面**



1.  自我介绍 
2.  链表怎么实现，数组和链表的区别，有数组的情况为什么要用链表； 
3.  介绍BFC，怎么触发 
4.  删除v-for渲染中的一项，如何操作 
5.  对data内的对象增加一个属性，实现响应式，如何操作 
6.  vue实现数据劫持的原理，简单介绍 
7.  直接通过数组下标修改元素能否实现响应式更新 
8.  移动端适配原理-rem 
9.  **key作用，哪些场景不能原地复用？**  
动画或者是过渡效果 
10.  用ES5实现ES6的map方法 
11.  new作用 
12.  this绑定规则 



**技术二面**



1.  自我介绍 
2.  离职原因 
3.  介绍下最近做的项目 
4.  项目中遇到过的难处理的问题 
5.  介绍下原型链 
6.  **proto**和prototype的区别 
7.  宏任务和微任务，事件循环 
8.  webpack，熟悉的常用配置项、loader作用、经过loader处理后是什么、经过webpack对项目做过哪些优化、 
9.  对一些线上问题调试解决的经验介绍 
10.  抓包、代理工具是否有了解 
11. 



##### 3、2021-03-29：部门负责人面


1. 自我介绍
2. 项目介绍
3. 自我规划
4. 学校了解
5. 学习方法

